// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://setsukaii.github.io',
	base: '/xuejiayi-portfolio', // GitHub Pages 必需
});
